interface:
  display_name: "CAD Skill"
  short_description: "Run staged interior-design workflows across plan, 2D SVG, BOM, and image outputs."
  default_prompt: "Use $cad-skill to produce the next CAD workflow artifact and save local outputs when needed."

policy:
  allow_implicit_invocation: true
